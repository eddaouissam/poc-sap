name: know the directory hh

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: know the dir
        run: pwd
      - name: list the subdir
        run: ls ..